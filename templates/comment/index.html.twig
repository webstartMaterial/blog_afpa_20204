{% form_theme commentForm 'bootstrap_5_layout.html.twig' %}

{{ form_start(commentForm, { 'attr': {'class': 'row mx-auto'} }) }}

    <div class="col-md-8 mx-auto">
        {{ form_row(commentForm.message) }}
        {{ form_row(commentForm._token) }}

        <button type="submit" class="btn btn-success mt-3">Ajouter un commentaire</button>
    </div>
{# render_rest : affiche pas le reste des autres champ dans mon formulaire #}
{{ form_end(commentForm, {render_rest: false}) }}
